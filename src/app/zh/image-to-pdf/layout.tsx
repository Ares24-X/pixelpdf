import { Metadata } from "next";
export const metadata: Metadata = { title: "图片转PDF - 在线图片合成PDF" };
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
