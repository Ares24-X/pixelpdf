import { Metadata } from "next";
export const metadata: Metadata = { title: "旋转PDF - 在线旋转PDF页面" };
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
