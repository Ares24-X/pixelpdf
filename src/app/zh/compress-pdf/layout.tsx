import { Metadata } from "next";
export const metadata: Metadata = { title: "压缩PDF - 在线压缩PDF文件大小" };
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
