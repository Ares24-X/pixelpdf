import { Metadata } from "next";
export const metadata: Metadata = { title: "PDF解密 - 在线PDF密码移除" };
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
