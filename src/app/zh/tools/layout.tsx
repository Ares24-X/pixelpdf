import { Metadata } from "next";
export const metadata: Metadata = { title: "PDF在线工具 - 全部免费" };
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
