import { Metadata } from "next";
export const metadata: Metadata = { title: "PDF加密 - 在线PDF密码保护" };
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
