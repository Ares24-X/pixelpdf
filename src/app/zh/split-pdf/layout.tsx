import { Metadata } from "next";
export const metadata: Metadata = { title: "拆分PDF - 在线拆分PDF页面" };
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
